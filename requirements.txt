# xyz-robot-data-collector (xrdc) dependencies
#
# Setup:
#   uv venv --python /usr/bin/python3.10 .venv_xrdc
#   uv pip install -r requirements.txt
#
# ROS2 packages (rclpy, sensor_msgs, cv_bridge) come from /opt/ros/humble via
# PYTHONPATH — sourced by start_recorder.sh, NOT installed in the venv.
# numpy<2 is required for ROS2 Humble compatibility.

numpy<2
opencv-python-headless
google-api-python-client
google-auth
google-auth-oauthlib
google-auth-httplib2
